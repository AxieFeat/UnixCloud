<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" packages="net.unix" shutdownHook="disable">
    <Appenders>
        <Queue name="GuiConsole">
            <PatternLayout pattern="[%d{HH:mm:ss} %level]: %msg%n" />
        </Queue>
        <TerminalConsole name="TerminalConsole">
            <PatternLayout>
                <LoggerNamePatternSelector defaultPattern="%highlightError{[%d{HH:mm:ss} %level]: [%logger] %colorFormatting{%msg}%n%xEx{full}}">
                    <PatternMatch key=",net.unix"
                                  pattern="%highlightError{[%d{HH:mm:ss} %level]: %colorFormatting{%msg}%n%xEx{full}}" />
                </LoggerNamePatternSelector>
            </PatternLayout>
        </TerminalConsole>
        <RollingRandomAccessFile name="File" fileName="logs/latest.log" filePattern="logs/%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <LoggerNamePatternSelector defaultPattern="[%d{HH:mm:ss}] [%t/%level]: [%logger] %colorFormatting{%msg}{strip}%n%xEx{full}">
                    <PatternMatch key=",net.unix"
                                  pattern="[%d{HH:mm:ss}] [%t/%level]: %colorFormatting{%msg}{strip}%n%xEx{full}" />
                </LoggerNamePatternSelector>
            </PatternLayout>
            <Policies>
                <TimeBasedTriggeringPolicy />
                <OnStartupTriggeringPolicy />
            </Policies>
            <DefaultRolloverStrategy max="1000"/>
        </RollingRandomAccessFile>
    </Appenders>
    <Loggers>
        <Root level="info">
            <filters>
                <MarkerFilter marker="NETWORK_PACKETS" onMatch="DENY" onMismatch="NEUTRAL" />
            </filters>
            <AppenderRef ref="File"/>
            <AppenderRef ref="TerminalConsole" level="info"/>
            <AppenderRef ref="GuiConsole" level="info"/>
        </Root>
    </Loggers>
</Configuration>
